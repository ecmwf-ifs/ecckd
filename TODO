SCRIPTS

Store LBL flux calculation at 50 cm-1 in SW and 10 cm-1 in LW and use
to train any CKD model

Produce "high accuracy" CKD models with 64 points

CODE

find_g_points may be faster if minimizing (max(E)-min(E))/E_tol rather
than (max(E)-min(E))/mean(E)

ECRAD

